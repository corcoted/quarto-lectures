name: lecture-notes

channels:
  - conda-forge
  - defaults

# adjust below as needed
dependencies:
  - python
  - ipykernel
  - jupyter # can probably omit this, but it makes rendering easier
  - jupyter-cache # speeds up development
  - matplotlib
  - numpy
  - scipy
  - sympy
